import { Metadata } from "next"

import Page from "./page"

export const metadata: Metadata = {
  title: "SafeFrames | SafeMeme Labs",
  description:
    "Create a safe and secure ERC-20 token (with no coding required) in seconds on the best token generator on multiple blockchains.",
}
export default Page
