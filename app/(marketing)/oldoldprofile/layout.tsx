import { Metadata } from "next"

import MyProfile from "./page"

export const metadata: Metadata = {
  title: "My Profile | SafeMeme Labs",
  description: "Check out my profile at SafeMeme Labs",
}
export default MyProfile
