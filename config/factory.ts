import { FactoryConfig } from "types"

export const factoryConfig: FactoryConfig = {
  mainNav: [
    {
      title: "Create Token",
      href: "/factory",
    },
    {
      title: "My Tokens",
      href: "/mytokens",
    },
  ],
}
